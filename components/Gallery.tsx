'use client';
import {useRef,useState,useEffect} from 'react';
import {X,ArrowLeft,ArrowRight} from 'lucide-react';
import {photo} from '@/lib/content';
export default function Gallery({images}:{images:{id:number,label:string}[]}){const dialog=useRef<HTMLDialogElement>(null);const [active,setActive]=useState(0);const next=(delta:number)=>setActive(i=>(i+delta+images.length)%images.length);useEffect(()=>{function key(e:KeyboardEvent){if(!dialog.current?.open)return;if(e.key==='ArrowLeft')next(-1);if(e.key==='ArrowRight')next(1)}window.addEventListener('keydown',key);return()=>window.removeEventListener('keydown',key)},[images.length]);return <><div className="gallery-grid">{images.map((im,i)=><button key={i} className={i===0?'gallery-wide':''} onClick={()=>{setActive(i);dialog.current?.showModal()}}><img src={photo(im.id)} alt={im.label} loading="lazy"/><span>{im.label} <span>Увеличить ↗</span></span></button>)}</div><dialog ref={dialog} className="lightbox" onClick={e=>{if(e.target===e.currentTarget)dialog.current?.close()}}><button className="lightbox-close" aria-label="Закрыть галерею" onClick={()=>dialog.current?.close()}><X/></button><img src={photo(images[active].id)} alt={images[active].label}/><div><button aria-label="Предыдущее фото" onClick={()=>next(-1)}><ArrowLeft/></button><span>{images[active].label} · {active+1} / {images.length}</span><button aria-label="Следующее фото" onClick={()=>next(1)}><ArrowRight/></button></div></dialog></>}
