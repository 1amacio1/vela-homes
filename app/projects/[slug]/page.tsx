import {notFound} from 'next/navigation';
import {Header,Footer,Analytics} from '@/components/Chrome';
import {ProjectCard} from '@/components/Projects';
import Gallery from '@/components/Gallery';
import {projects,photo} from '@/lib/content';
export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const slug=(await params).slug;const item=projects.find(p=>p.slug===slug);return {title:item?`${item.name} · ${item.area} м² — VELA`:'Проект не найден'}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=projects.find(p=>p.slug===slug);if(!p)notFound();const index=projects.indexOf(p);return <><Header solid/><main className="inner-page"><section className="project-intro section"><a className="back-link" href="/projects">← Все проекты</a><div className="section-heading"><div><p className="eyebrow">{p.location}</p><h1>{p.name}<em>.</em></h1></div><p>{p.description}</p></div><div className="project-specs"><div><strong>{p.area} м²</strong><span>площадь дома</span></div><div><strong>{p.floors}</strong><span>{p.floors===1?'этаж':'этажа'}</span></div><div><strong>{p.material}</strong><span>технология строительства</span></div><a className="button" href="/#contact">Хочу похожий дом ↗</a></div></section><img className="project-cover" src={photo(p.image)} alt={'Дом «'+p.name+'»'}/><section className="section project-gallery"><div className="section-heading"><h2>Продуман <em>целиком.</em></h2><div><p className="eyebrow">В ЭТОМ ПРОЕКТЕ ВЫПОЛНЕНО</p><div className="pills">{['Строительство дома','Внутренняя отделка','Инженерные системы','Дизайн интерьера','Благоустройство','Ландшафт'].map(s=><span key={s}>{s}</span>)}</div></div></div><Gallery images={[{id:p.image,label:'Архитектура и фасад'},{id:[12998902,7546713,7166931][index%3],label:'Интерьер и отделка'},{id:[7166931,12998902,7546713][index%3],label:'Материалы и детали'},{id:5563466,label:'Терраса и пергола'},{id:35070095,label:'Озеленение и пространство для отдыха'}]}/><p className="demo-note">Демонстрационная концепция. Фотографии в галерее — подборка референсов разных объектов, показывающая возможные решения.</p><div className="catalog-end"><h2>Близко <em>по духу?</em></h2><a className="button" href="/#calculator">Рассчитать свой дом ↗</a></div><div className="section-heading"><h2>Другие <em>истории.</em></h2></div><div className="project-grid">{[projects[(index+1)%12],projects[(index+2)%12]].map(p=><ProjectCard key={p.slug} project={p}/>)}</div></section></main><Footer/><Analytics/></>}
